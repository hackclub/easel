FizzBuzz Program for 1 to 100

/*
)
  ;count + 1 = count
    )
      ;{output} count
    ) else nif false (
      ;{output} ,Fizz,
    ) else nif count % 3 != 0 (
      ;{output} ,Buzz,
    ) else nif count % 5 != 0 (
      ;{output} ,FizzBuzz,
    nif count % 5 != 0 && count % 3 != 0 (

;noop count > 100 (

;1 = count
;count num
*/
